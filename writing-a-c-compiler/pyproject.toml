[project]
name = "writing-a-c-compiler"
version = "0.1.0"
description = "integration tests for writing-a-c-compiler"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=9.0.3",
    "pytest-xdist>=3.8.0",
]

[dependency-groups]
dev = ["ruff>=0.15.15", "ty>=0.0.40"]
