# MySQL Schema to struct converter

Reads a MySQL schema and converts it to a Rust struct. Can be installed via `cargo install --path .` and then invoked in a terminal with. `msts`. Expects the use of `serde`.
